# docs/prereg/archive/ — NOT EVIDENCE

Superseded preregistration versions live here. Nothing in this folder may be cited as
evidence that a prediction was fixed in advance.

## PREREG_evo1_broadcast v1 — absent by decision

v1 is **not** archived, because it does not exist in any verifiable form. The whole
`paper-salvage/` tree was untracked until commit `e705497`, which already contains v2, so
no blob of v1 is in the object store; the working-tree file was overwritten before any
commit was made. A transcript reconstruction was briefly filed here and has been
**removed**: a prereg reconstructed after the fact is not a prereg, and keeping it invites
exactly the citation it must not receive.

The record is the plain statement in `../PREREG_evo1_broadcast.md` §Provenance:

> The impulse protocol was revised twice during instrument validation. Only the final
> protocol (v2, locked `<hash>`, `<date>`) was preregistered; earlier versions were not.
