model_id: "GenerTeam/GENERator-v2-prokaryote-3b-base"
model_type: "generator"
hf_trust_remote_code: true
# GENERator prokaryote variant — same LlamaForCausalLM architecture as eukaryote.
# Sequences must be multiples of 6 bp (6-mer tokenizer) — handled in wrapper.
# Expected MLP structure: standard GPT-style down_proj
down_proj_pattern: "model.layers.{i}.mlp.down_proj"   # confirmed: LlamaForCausalLM
num_layers: 30                                          # same architecture, 30 hidden layers
dtype: "float32"
device: "cuda"
causal: true
